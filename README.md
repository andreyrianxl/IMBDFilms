# IMBDFilms API

API desenvolvida em Java com Spring Boot para o gerenciamento de filmes
O que tem no código:

-CRUD completo de filmes.

-Validação de dados para evitar lixo no banco (`@NotBlank`, `@NotNull`).

-Tratamento global de erros devolvendo listas detalhadas em JSON.
